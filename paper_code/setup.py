import setuptools

setuptools.setup(
    name='neurips_code', # Replace with your own username
    version='1.0.0',
    author='',
    author_email='',
    description='neurips_code package',
    packages=setuptools.find_packages(),
    python_requires='>=3.7'
)
